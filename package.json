{
  "name": "combobreaker",
  "private": true,
  "description": "Dev tooling for the ComboBreaker extension. The extension itself has no build step and no runtime dependencies.",
  "scripts": {
    "test": "node --test",
    "lint": "eslint .",
    "check": "node scripts/check_manifest.cjs",
    "pack": "node scripts/pack.cjs",
    "smoke": "node scripts/smoke_overlay.js",
    "blocklist": "node scripts/update_blocklist.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
